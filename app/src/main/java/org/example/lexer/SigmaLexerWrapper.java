package org.example.lexer;

/**
 * Stubbed lexer wrapper. ANTLR removed; the RD parser does its own tokenization.
 */
public class SigmaLexerWrapper {
    // No-op stub kept for compatibility with older APIs.
}
